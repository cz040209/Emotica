# Emotica Frontend

React + Vite frontend for the Emotica voice AI interface.

## Commands

- `npm install`
- `npm run dev`
- `npm run build`
- `npm run preview`

## Notes

- The UI streams audio to the orchestrator API over WebSocket.
- Configure the frontend endpoint in source if you deploy to a different host/port.
